  {{#each posts}}
  <li>
    <article class="post">
      <div class="title">
        <h2>{{title}}</h2>
      </div>
      <div>
        {{post_content}}
        <div class="post-content">
            by {{user.username}} | {{created_at}} |
            <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
        </div>
      </div>
    </article>
  </li>
  {{/each}}